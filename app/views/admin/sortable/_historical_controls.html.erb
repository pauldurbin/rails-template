<small>
  <%= link_to 'Edit', edit_admin_historical_event_path(id: item.id) %>
  |
  <%= link_to 'Delete', admin_historical_event_path(id: item.id), method: :delete, data: { confirm: 'Are you sure?' } %>
</small>
